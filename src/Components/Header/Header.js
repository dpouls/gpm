import React from "react";
import "./Header.scss";

const Header = (props) => {
  return (
    <div className="header-container">
      <div>GOLDEN</div>
      <i class="fas fa-bars"></i>
    </div>
  );
};

export default Header;
